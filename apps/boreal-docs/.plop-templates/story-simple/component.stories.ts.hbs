import { html, css } from 'lit';
import { formatHtmlSource } from '@/utils/formatters';
import type { BorealStory, BorealStoryMeta } from '@/types/stories';

type StoryArgs = {
  // TODO: Define your component args here
  // Example:
  // label: string;
  // disabled: boolean;
  // variant: 'primary' | 'secondary';
};
type Story = BorealStory<StoryArgs>;

const meta = {
  title: '{{titlePath}}',
  component: '{{componentName}}',
  parameters: {
    docs: {
      source: {
        excludeDecorators: true,
        transform: formatHtmlSource,
      },
    },
    __sb: {
      // TODO: Add custom styling here if needed. Otherwise, delete this section.
      // Applied to the story wrapper to prevent inline styles. See `withCustomStyling` decorator in `preview.ts`
      // Example:
      // backgroundColor: '#f0f0f0',
      // padding: '20px',
      // marginBlock: '10px',
    },
  },
  argTypes: {
    // TODO: Add your argTypes configuration here
{{#if includeCategories}}
    // Example with categories:
    // label: {
    //   control: 'text',
    //   description: 'The label text',
    //   table: {
    //     category: 'Configuration',
    //     type: { summary: 'string' },
    //     defaultValue: { summary: 'Click me' },
    //   },
    // },
    // disabled: {
    //   control: 'boolean',
    //   description: 'Disables the component',
    //   table: {
    //     category: 'State',
    //     type: { summary: 'boolean' },
    //     defaultValue: { summary: 'false' },
    //   },
    // },
{{else}}
    // Example:
    // label: { control: 'text' },
    // disabled: { control: 'boolean' },
{{/if}}
  },
  args: {
    //TODO: Add default args here. They can be overridden in each story.
    // Example:
    // label: 'Click me',
    // disabled: false,
  },
} satisfies BorealStoryMeta<StoryArgs>;

const styles = css`
  /**
   * TODO: Add your component class-based styles here if needed. Otherwise, delete this section.
   * Avoid using inline styles in the render function for better maintainability.
   */
  .component-class {
    /* Component styles */
  }
`;

export default meta;

/**
 * Reusable render function for the {{componentNameWithoutPrefix}} component stories.
 * @param args - The story arguments containing component props
 * @returns An HTML template for the {{componentNameWithoutPrefix}} component
 */
const render{{componentNameWithoutPrefix}}: Story['render'] = args => html`
  <!-- 🚨 Make sure to remove the comments below when implementing the component markup to avoid rendering issues 🚨 -->
  <!-- TODO: Remove the <style> tag below and the 'styles' variable if custom component styles are not required -->
  <style>
    ${styles}
  </style>
  <!-- TODO: Implement your component markup here -->
  <!-- Component tag: <{{componentName}}> -->
  <{{componentName}}>
    <!-- Add your component content here -->
  </{{componentName}}>
  `;

{{#each stories}}
/**
 * {{this}} story variant
 */
export const {{this}}: Story = {
  args: {
    // TODO: Override default args for the {{this}} story if needed, otherwise remove this section.
  },
  // TODO: If the render function is different for this story, define it here. Otherwise, it will use the default render function.
  // Example:
  // render: (args) => html`
  //  <{{componentName}} custom-prop=${args.customProp}></{{componentName}}>
  // `
  render: render{{../componentNameWithoutPrefix}},
};

{{/each}}
