import {
  Meta,
{{#if (eq storyDisplayMode "manual")}}
  Canvas,
{{else}}
  Stories,
{{/if}}
  ArgTypes,
  Title,
  Subtitle,
} from '@storybook/addon-docs/blocks';
import LinkTo from '@storybook/addon-links/react';
import { StoryName, Callout } from '@/components/docs';
import * as {{componentNamePascal}}Stories from './{{componentName}}.stories';

<Meta of={ {{~componentNamePascal}}Stories} />

<Title of={ {{~componentNamePascal}}Stories} />

{{description}}

<Subtitle>How to use it</Subtitle>

1. Import the component into your project.

```ts
// TODO: Update import path and component name
import { {{componentNameWithoutPrefix}} } from 'boreal-ds/{{componentName}}';
```

2. Use the `<{{componentName}}>` tag to display the component in your HTML.

```html
<!-- TODO: Add component usage example with props -->
<{{componentName}}></{{componentName}}>
```

<Subtitle>When to use it</Subtitle>

{/* TODO: Add guidance on when to use this component */}
Use the {{componentNameWithoutPrefix}} component when...

<Subtitle>Component preview</Subtitle>

The following examples showcase the core features and visual options of the {{componentNameWithoutPrefix}} component.

<Callout variant="tip" icon="💡">
  You can click on the **Show code** button in the bottom-right section of the following code
  snippets to see how to use the component.
</Callout>

{{#if (eq storyDisplayMode "manual")}}
{/*
  MANUAL MODE: Stories are listed explicitly below.

  ⚠️  If you add/remove stories in the {{componentName}}.stories.ts file,
      remember to update this list manually or regenerate the MDX file.

  💡  Tip: Consider switching to automatic mode by using <Stories /> instead,
      which automatically discovers all stories at runtime and stays in sync.

  👉  Feel free to remove this comment after reviewing.
*/}
{{#each stories}}
<StoryName of={ {{~../componentNamePascal}}Stories.{{this}} } />
<Canvas of={ {{~../componentNamePascal}}Stories.{{this}} } />

{{/each}}
{{else}}
{/*
  AUTOMATIC MODE: The <Stories /> component automatically discovers and renders
  all stories from {{componentName}}.stories.ts at runtime.

  ✅  Always in sync - no manual updates needed when adding/removing stories

  👉  Feel free to remove this comment after reviewing.
*/}
<Stories />
{{/if}}

<Subtitle>Accessibility</Subtitle>

{/* TODO: Add accessibility information */}
The {{componentNameWithoutPrefix}} component is designed with accessibility in mind. It includes appropriate ARIA attributes and keyboard navigation support to ensure that all users can interact with it effectively.

<Subtitle>Properties</Subtitle>

<ArgTypes of={ {{~componentNamePascal}}Stories} />

<Subtitle>Interact with the component</Subtitle>

To interact with the component, test actions, verify accessibility compliance, and perform visual testing, navigate to the <LinkTo title={ {{~componentNamePascal}}Stories.default.title} story="default">Default</LinkTo> section.
