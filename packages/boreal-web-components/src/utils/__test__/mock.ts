/*
    Example mock file, you can delete it if you don't need it.
*/

import { format } from '../helpers/utils';

export const mockedFunction = () => 'mocked value';

export const format2 = format('MM-DD-YYYY');
